import React from "react";

const CarbonScore = ({ totalCarbonScore }) => (
  <div className="carbon-score">
    <h2>Total Carbon Score</h2>
    <p>{totalCarbonScore}</p>
  </div>
);

export default CarbonScore;
